meta {
  name: App_Reset
  type: http
  seq: 3
}

delete {
  url: http://localhost:{{port}}/reset
  body: none
  auth: bearer
}

auth:bearer {
  token: {{globalJwtKey}}
}

settings {
  encodeUrl: true
  timeout: 0
}
